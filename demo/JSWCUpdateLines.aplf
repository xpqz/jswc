 JSWCUpdateLines args;rows;cols;size;posn;viz;obs;form;ctl
 (form ctl)←'.'(≠⊆⊢)⊃args

 :Select ctl
 :Case 'GO'
     (rows cols)←(form∘,¨'.ROWS' '.COLS')eWG¨⊂'Value'
     (posn size)←(100 10)((form eWG'Size')-110 20)
     demoDrawBoxes form posn size rows cols
 :Case 'CLR'
     eEX form,'.BX'
 :Case 'HIDE'
     viz←~(form,'.HIDE') eWG 'State'
     obs←form∘,¨(0=⎕NC form,'.BX')↓'.BX' '.LOGO'
     obs eWS¨⊂'Visible' viz
 :EndSelect
