 {R}←∆EX names;caller;msg;ex;EX;z
 caller←⊃⎕RSI
 :If 0=⎕NC'Mode' ⋄ Mode←0 ⋄ :EndIf
 'Mode=0 only at this point'⎕SIGNAL(Mode≠0)/11

 EX←⎕NS''
 ex←EX.EX←⎕NS''
 ex.ID←,⊆names

 R←caller.⎕EX names
 msg←⎕JSON EX

 :If CONNECTED
     z←CONN #.w.Send ⎕←msg
 :Else
     ⎕←msg
 :EndIf
