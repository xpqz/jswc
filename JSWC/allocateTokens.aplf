allocateTokens;z

:Trap 2 ⍝ SYNTAX ERROR in v18.2
     z←↑⎕TALLOC 0
     :If (≢z)≥i←z[;2]⍳⊂t
         TOKENRANGE←z[i;1]
     :Else
         TOKENRANGE←⎕TALLOC 1 t
     :EndIf
     WG_TOKEN←TOKENRANGE+0.1
 :Else
     WG_TOKEN←1818
 :EndTrap
