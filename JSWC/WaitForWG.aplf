R←WaitForWG Props;j;⎕TRAP;p

j←⎕TGET WG_TOKEN

:If 9=⎕NC 'j.WG'
:AndIf ∧/(p←,⊆Props)∊j.WG.Properties.⎕NL -2
   R←j.WG.Properties⍎((1=≢p)/',⊂'),⍕p
   R←j.WG.ID MixTables p R
   :If 1=≡Props
       R←⊃R ⍝ A single simple property
   :EndIf
:Else
   ∘∘∘ ⍝ Didn't get the expected properties back
:EndIf
