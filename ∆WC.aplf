 {R}←Name ∆WC Props;p;ps;m;name;WC;msg;length;m2;m3
 R←Name ⎕WC Props

 p←(≢Props)↑ps←R ⎕WG'Proplist'
 m←~((length←≢¨Props)∊2 3)∧(name←⊃¨Props)∊ps
 (m/name)←m/p
 m2←(~m)∧length=2
 (m2/Props)←2⊃¨m2/Props
 m3←(~m)∧length=3
 (m3/Props)←1↓¨m3/Props

 WC←⎕NS''
 WC.WC←⎕NS ''
 WC.WC.(ID Properties)←Name(⎕NS'')
 ⍎'WC.WC.Properties.(',(⍕name),')←Props'

 msg←⎕JSON⍠'HighRank' 'Split'⊢WC

 :If CONNECTED
     CONN w.Send msg
 :Else
     ⎕←'SRV:',msg
 :EndIf
